"""Infrastructure layer — concrete implementations of domain ports.

Each sub-package corresponds to an external technology (Ollama, FAISS, PyPDF)
and provides adapters that implement the abstract base classes defined in
``domain.ports``.
"""